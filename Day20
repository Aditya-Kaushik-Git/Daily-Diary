#### **Day 20: Collections Framework - Set and Map**
```markdown
# Day 20: Handling Unique Data and Key-Value Pairs

**Date:** [Insert Date]

**Topics Covered:**
1. `HashSet` (Unique elements, unordered).
2. `HashMap` (Key-Value pairs).
3. Iterating over Maps using `entrySet()`.
4. Difference between List, Set, and Map.

**Learning Summary:**
Today I completed the basics of Collections. I learned that `Set` is used when we want unique elements (no duplicates allowed). `Map` is crucial for storing data in Key-Value pairs (like a dictionary). `HashMap` is extremely fast for looking up values if we know the key.

**Code Snippet:**
```java
import java.util.HashSet;
import java.util.HashMap;

public class Day20 {
    public static void main(String[] args) {
        // SET: Unique elements only
        HashSet<Integer> ids = new HashSet<>();
        ids.add(101);
        ids.add(101); // Duplicate ignored
        System.out.println("Unique IDs: " + ids);

        // MAP: Key-Value pairs
        HashMap<String, Integer> students = new HashMap<>();
        students.put("Amit", 90);
        students.put("Rahul", 85);
        
        // Accessing value by key
        System.out.println("Amit's Marks: " + students.get("Amit"));
    }
}
